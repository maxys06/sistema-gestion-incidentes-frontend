export default class Problema {
  constructor(id, tipo, descripcion, tiempoMaximoResolucion, complejo) {
    this.id = id;
    this.tipo = tipo;
    this.descripcion = descripcion;
    this.tiempoMaximoResolucion = tiempoMaximoResolucion;
    this.complejo = complejo
  }
}